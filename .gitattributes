# 폰트는 바이너리. git 이 diff 를 시도하거나 개행을 변환하지 않도록 명시한다.
*.ttf binary
*.otf binary

# MobaXterm 은 Windows 프로그램이라 CRLF 를 유지해야 한다.
# binary(= -text -diff): 개행 변환을 막고, raw 가 octet-stream 으로 내려보내
# 브라우저에서 바로 다운로드되게 한다.
*.mxtcolors binary
